<div class="container">
    <div class="row">
        <div class="col">
            
            <ol class="list-group">
                {{#each shirt}}
                    {{#if mens}}
                        <li class="list-group-item">
                                {{id}}
                        </li>
                    {{/if}}
                {{/each}}
            </ol>
            
        </div>
    </div>
</div>
